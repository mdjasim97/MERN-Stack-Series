
const Phone = ({phones}) => {
    // console.log(phones)
    // const name = phones.phone_name;
    // const phonePrice = phones.slug
    // const price = parseInt(phonePrice.split("-")[1])
    return (
        <div>
            {
                
            }
        </div>
    );
};

export default Phone;